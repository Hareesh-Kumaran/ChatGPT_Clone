{
    "buildCommand":"cd Client && cd ChatGPT-client && npm install && ./node_modules/vite/bin/vite.js build",
    "outputDirectory":"client/dist",
    "framework":"vite",
    "rewrites":[
        {
            "source":"/api/(.*)",
            "destination":"/Server/server.js"
        }
    ]
}
